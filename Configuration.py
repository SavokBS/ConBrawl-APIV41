
# DisableNagle: reduce delay and improve performance between client and server BUT require more power #

settings = {
    "DisableNagle": True,
    "PrintEnabled": True,
    "UseContentUpdater": False,
    "Proxy": False,
    "DumpPacket": False,
    "Blacklist": [24109],
    "DumpMajor": 41,
    "Tag": "Y92PLC2",
    "ApiToken": "YOUR_TOKEN"
    

    #Put the tag ONLY FULL ACCOUNT, if there are less than 54 brawlers on the account, the server won't work!
    #Поставьте тэг ТОЛЬКО ФУЛЛ АККАУНТА, если на аккаунте меньше 54 бойцов, сервер не будет работать!
}
